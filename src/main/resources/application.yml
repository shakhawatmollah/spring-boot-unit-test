server:
  port: 9000
  servlet:
    context-path: /
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/spring_unit_test
    username: root
    password: 
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    database-platform: org.hibernate.dialect.MySQLDialect
    show-sql: true
    generate-ddl: true
    hibernate:
      ddl-auto: update
